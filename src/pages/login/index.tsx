import {Authorization} from "../../components/Authorization/Authorization";


export const Login = () => {
    return (
        <div>
            <Authorization type={'login'}/>
        </div>
    );
};
